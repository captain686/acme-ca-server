# Marker file to make `tests` importable as a package.
# This prevents the `tests/pytest` directory from becoming a top-level
# package named `pytest` (which would clash with the real pytest library).
